# OpenAI API settings
OPENAI_API_KEY=your-api-key-here       # Your OpenAI API key
OPENAI_BASE_URL=https://api.openai.com/v1  # API endpoint (change for compatible APIs)
OPENAI_MODEL=gpt-4                     # Model to use

# Local repo settings
REPO_PROVIDER=local
REPO_PATH=/path/to/your/repo           # Absolute path to the Git repository to monitor

# GitHub provider settings (authenticates via OAuth device flow at startup)
# REPO_PROVIDER=github
# GITHUB_OWNER=owner-name              # Repository owner (username or organization)
# GITHUB_REPO=repo-name                # Repository name

# Gerrit provider settings (no authentication required for public repos)
# REPO_PROVIDER=gerrit
# GERRIT_HOST=chromium-review.googlesource.com  # Gerrit server host
# GERRIT_PROJECT=chromium/src                    # Project name

# Azure DevOps provider settings (authenticates via DefaultAzureCredential)
# REPO_PROVIDER=ado
# ADO_ORGANIZATION=your-org                # Azure DevOps organization name
# ADO_PROJECT=your-project                 # Azure DevOps project name
# ADO_REPOSITORY=your-repo                 # Repository name or ID

# RepoSentinel settings
BRANCH=main                            # Branch to analyze
CHECK_INTERVAL_HOURS=1                 # How often to run (in hours)
MAX_FETCH_HOURS=168                    # Maximum lookback window (in hours), used on first run or as cap
REPORT_DIR=./reports                   # Directory to save generated reports

# Optional settings
# SUB_PATH=src/tools,src/helpers        # Comma-separated paths to scope analysis (omit for full repo)
# CUSTOM_PROMPT_FILE=./custom-prompt.md # Optional: Path to file containing custom instructions (appended to system prompt)
